# 吉达港口 3D 数字孪生 — 演示版技术实现文档

本文档说明如何从零实现**客户演示用 PoC**：以 **React + CesiumJS** 为核心的大屏三维可视化，配合 **极简 Node 后端** 提供模拟统计数据与可选实时位置推送。技术选型与《吉达港口3D数字孪生系统项目方案》一致，范围对应方案中的**快速演示模式**与**第一阶段**的缩减形态。

---

## 1. 目标与范围

### 1.1 目标

- 浏览器全屏或可固定分辨率（如 1920×1080）展示港口区域三维地球/地形/影像。
- 中央为 Cesium 主视图；周边为可折叠的指标面板（在港船舶数、吞吐量示意、告警列表等）。
- 船舶、岸桥、集卡等以**低负载方式**呈现（图标/点/简单几何 + 状态色），即方案中的**快速演示模式**。
- 后端提供 **REST JSON**；可选 **WebSocket** 模拟秒级位置更新。

### 1.2 非目标（PoC 不实现）

- 真实 AIS / TOS / SCADA 对接。
- 微服务拆分、API 网关、K8s、消息队列、预测分析服务。
- 完整 3D Tiles 港机精模与模型管理服务（可作为后续阶段说明）。

---

## 2. 技术架构

```
+----------------------------------------------------------+
|                    浏览器（大屏前端）                      |
|  React + Vite + TypeScript + Tailwind（可选）             |
|  CesiumJS Viewer + Entity / GeoJSON / 可选 CZML           |
+----------------------------------------------------------+
                            | HTTP REST / 可选 WebSocket
                            v
+----------------------------------------------------------+
|              Node.js + Express（演示后端）                 |
|  /api/stats /api/ships  /api/alerts  …                   |
|  WebSocket：模拟 port-data（可选）                         |
+----------------------------------------------------------+
```

数据流与方案 **§5.3** 一致时可只实现子集：`ship_update` 等少量 `type`，`payload` 结构保持可扩展。

---

## 3. 技术栈版本建议

| 类别 | 技术 | 建议版本 | 说明 |
|------|------|----------|------|
| 运行时 | Node.js | 18 LTS+ | 与方案一致 |
| 前端框架 | React | 18.x | |
| 语言 | TypeScript | 5.x | |
| 构建 | Vite | 5.x | |
| 三维 | CesiumJS | 1.110+ | 与方案 1.108 同族；宜随官方升级 |
| 状态 | Zustand | 4.x | 可选；简单场景可用 React state |
| 后端 | Express | 4.x | 演示足够 |

---

## 4. 工程目录结构

推荐 **monorepo 双包** 或 **根目录两文件夹**，便于一条命令分别启动前后端。

```
Jeddah-Port-Digital-Twin-System-Mock/
├── 吉达港口3D数字孪生系统项目方案.md
├── 演示版技术实现文档.md          # 本文档
├── frontend/                      # Vite + React + Cesium
│   ├── public/
│   ├── src/
│   │   ├── main.tsx
│   │   ├── App.tsx
│   │   ├── components/
│   │   │   ├── layout/ScreenShell.tsx    # 顶栏 + 左中右布局
│   │   │   ├── cesium/CesiumViewport.tsx # Viewer 封装
│   │   │   └── widgets/StatsPanel.tsx
│   │   ├── cesium/
│   │   │   ├── viewerConfig.ts           # Ion token、初始相机
│   │   │   ├── layers.ts                 # 图层开关逻辑
│   │   │   └── mockEntities.ts           # 从 API 创建 Entity
│   │   └── api/
│   │       └── client.ts                 # fetch + ws
│   ├── index.html
│   ├── vite.config.ts
│   ├── package.json
│   └── tsconfig.json
├── backend/
│   ├── src/
│   │   ├── index.ts                      # Express 入口
│   │   ├── routes/
│   │   │   └── api.ts
│   │   ├── mock/
│   │   │   ├── stats.ts
│   │   │   └── ships.ts                  # 初始船位 + 简单漂移算法
│   │   └── ws/
│   │       └── broadcast.ts              # 可选
│   ├── package.json
│   └── tsconfig.json
└── README.md                            # 启动说明（可选，由你方维护）
```

---

## 5. 前端实现要点

### 5.1 创建工程

```bash
cd frontend
npm create vite@latest . -- --template react-ts
npm install cesium zustand
npm install -D vite-plugin-cesium
```

### 5.2 Vite 与 Cesium

- 使用 **`vite-plugin-cesium`** 处理 Cesium 静态资源与 worker 路径。
- `vite.config.ts` 中配置 `cesium()` 插件，并设置 `define` 若需 `CESIUM_BASE_URL`（以插件文档为准）。
- **Cesium Ion**：在 [Cesium ion](https://cesium.com/ion/) 创建 token，写入环境变量，例如 `VITE_CESIUM_ION_TOKEN`，仅在构建/开发时注入前端（演示环境可接受；生产应走自有后端代理或受限 key策略）。

### 5.3 初始视角（吉达港区域）

在 `viewerConfig.ts` 中配置 `Viewer` 创建后执行：

- `camera.flyTo` 或 `setView`，目标为吉达伊斯兰港附近经纬度、高度、俯仰角（具体数值可用公开地图量测后固定为常量）。
- 使用 **World Terrain**（Ion）或方案 **§6** 中的降级方案；影像用 Ion 默认或 Bing，视 token 权限而定。

### 5.4 快速演示模式图层

| 图层 | PoC 实现方式 |
|------|----------------|
| 地形/影像 | Ion 默认地形 + 影像图层 |
| 船舶 | `viewer.entities.add`：Billboard + Label，或 Point样式 |
| 岸桥/集卡 | 同上，不同图标或颜色 |
| 堆场/泊位 | `GeoJsonDataSource.load` 加载静态 `public/data/*.geojson` |

图层开关：用 React state 或 Zustand 保存布尔值，在 `CesiumViewport` 内 `useEffect` 中 `entity.show = flag` 或对 DataSource `show` 赋值。

### 5.5 调用后端

- **开发环境**：Vite `server.proxy` 将 `/api` 与 `/ws` 代理到 `http://localhost:3001`，避免 CORS。
- `GET /api/stats` → 更新侧栏数字。
- `GET /api/ships` → 首次绘制船舶 Entity。
- 若启用 WS：连接 `ws://localhost:3001/ws`，解析 JSON 后更新对应 `entity.position`（可用 `SampledPositionProperty` 或每帧直接设 `Cartesian3`）。

### 5.6 类型定义（与方案对齐的可扩展结构）

与方案 **§5.3** 兼容的简化消息示例：

```typescript
// 前端 shared类型（可抽到 packages/types 或 frontend/src/types）
export type PortDataType = 'ship_update' | 'stats_update'

export interface PortDataMessage<T = unknown> {
  type: PortDataType
  timestamp: string
  payload: T
}

export interface ShipData {
  mmsi: string
  name: string
  position: { longitude: number; latitude: number }
  heading: number
  speed: number
  status: 'anchored' | 'moored' | 'underway'
}
```

---

## 6. 后端实现要点

### 6.1 创建工程

```bash
cd backend
npm init -y
npm install express cors ws
npm install -D typescript ts-node-dev @types/express @types/node @types/ws
```

`package.json` 脚本示例：`"dev": "ts-node-dev --respawn src/index.ts"`。

### 6.2 REST 设计

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 存活检查 |
| GET | `/api/stats` | 大屏 KPI：在港船数、今日吞吐量示意、作业效率等 |
| GET | `/api/ships` | 当前船舶列表（静态种子 + 可带简单随机偏移） |
| GET | `/api/alerts` | 告警列表（演示文案） |

响应示例 `GET /api/stats`：

```json
{
  "shipsInPort": 12,
  "throughputTeu": 8420,
  "berthUtilization": 0.76,
  "updatedAt": "2026-04-10T12:00:00.000Z"
}
```

### 6.3 WebSocket（可选）

- 路径如 `/ws`，定时（如每 2～5 秒）向所有客户端广播 `PortDataMessage`。
- `ship_update` 的 `payload` 使用 `ShipData`；前端根据 `mmsi` 匹配已有 Entity 更新位置。
- 演示人数少时单进程足够；无需 Redis。

### 6.4 CORS

开发阶段前端独立端口时，对 `http://localhost:5173` 开启 `cors()`；若全程走 Vite 代理，后端可仅监听 localhost。

---

## 7. 环境变量

| 变量 | 位置 | 说明 |
|------|------|------|
| `VITE_CESIUM_ION_TOKEN` | frontend | Cesium Ion 访问令牌 |
| `PORT` | backend | 默认 `3001` |
| `VITE_API_BASE` | frontend | 生产环境 API 根路径；开发可用代理留空 |

**安全提示**：Ion token 暴露在浏览器是常见做法用于演示；正式环境应限制 referrer、用量，或通过后端签发短期能力。

---

## 8. 构建与运行

```bash
# 终端 1
cd backend && npm run dev

# 终端 2
cd frontend && npm run dev
```

浏览器访问 Vite 提示的本地 URL（通常为 `http://localhost:5173`）。大屏演示建议使用 **全屏（F11）** 并关闭无关插件。

生产构建：`frontend` 执行 `npm run build`，静态资源由 Nginx 或 Express `static` 托管；API 同域反向代理。

---

## 9. 性能与体验

- 默认开启**快速演示模式**：控制 Entity 数量（如船舶 &lt; 50）。
- 动画更新频率：WS 间隔不宜低于 500 ms（演示2～5 s 即可）。
- 首次加载：可显示 Loading 直到 `tileLoadProgress` 或超时降级为低细节影像。

---

## 10. 与完整方案的对照

| 完整方案（文档） | 演示版 |
|------------------|--------|
| 微服务、网关、队列 | 单进程 Express |
| PostgreSQL / Redis | 无；内存 mock |
| 3D Tiles 精模 | GeoJSON + Billboard |
| 实时 AIS | 模拟 WebSocket |
| 预测分析 | 不在 PoC 范围 |

后续可按方案 **§2.6** 将数据层替换为真实数据服务，将静态 glTF 逐步换为 3D Tiles，并拆分仪表盘服务。

---

## 11. 参考资料

- CesiumJS：<https://cesium.com/learn/cesiumjs-learn/>
- Vite + Cesium 插件：以 `vite-plugin-cesium` 仓库说明为准
- 项目方案：`吉达港口3D数字孪生系统项目方案.md`

---

## 附录 A：GeoJSON 示例（泊位示意）

```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "properties": { "name": "Berth-A01" },
      "geometry": {
        "type": "Polygon",
        "coordinates": [[[39.1, 21.5], [39.101, 21.5], [39.101, 21.501], [39.1, 21.501], [39.1, 21.5]]]
      }
    }
  ]
}
```

（坐标请替换为吉达港真实测绘或地图量测结果。）

---

*文档版本：与演示版 PoC 同步；扩展功能以项目方案为准。*
